# Standard LaTeX Template

This workspace now contains a basic LaTeX template suitable for Overleaf or local compilation.

## Files

- `main.tex`: main document

## Suggested compiler

Use `XeLaTeX` because the template is based on `ctexart` and supports Chinese by default.
